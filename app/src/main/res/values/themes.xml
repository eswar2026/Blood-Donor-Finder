<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Main theme WITH ActionBar -->
    <!-- Used by ALL activities except MainActivity -->
    <style name="Theme.blooddonor"
        parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/blood_red</item>
        <item name="colorPrimaryVariant">@color/blood_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor"
            tools:targetApi="l">@color/blood_red_dark</item>
    </style>

    <!-- NoActionBar theme -->
    <!-- Used ONLY by MainActivity (has custom toolbar) -->
    <style name="Theme.blooddonor.NoActionBar"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blood_red</item>
        <item name="colorPrimaryVariant">@color/blood_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor"
            tools:targetApi="l">@color/blood_red_dark</item>
    </style>

    <!-- Splash theme — full red screen, no action bar -->
    <style name="Theme.blooddonor.Splash"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blood_red</item>
        <item name="android:statusBarColor">@color/blood_red</item>
        <item name="android:windowBackground">@color/blood_red</item>
    </style>

    <!-- Card style -->
    <style name="AppCardView"
        parent="Widget.MaterialComponents.CardView">
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="contentPadding">16dp</item>
    </style>

    <!-- Primary Button -->
    <style name="PrimaryButton"
        parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/blood_red</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Outline Button -->
    <style name="OutlineButton"
        parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/blood_red</item>
        <item name="android:textColor">@color/blood_red</item>
        <item name="cornerRadius">8dp</item>
    </style>

</resources>